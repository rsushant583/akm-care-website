/**
 * Retired from Layout (Phase 3.5): fixed ticker under the nav caused top-of-viewport clipping.
 * Daily quote now lives in page sections via DailyMotivation — keep this module for reference only.
 */
export default function GlobalMotivationLayer() {
  return null;
}
